files:
  "/tmp/.npmrc":
    mode: "000644"
    owner: root
    group: root
    content: |
      jobs=1
      unsafe-perm=true
container_commands:
  01-renameEnvFile:
    #command: "sudo cp .env.eb .env"
    command: "sudo cp .env.dev .env"
    #command: "sudo cp .env.prod .env"
option_settings:
  aws:elasticbeanstalk:container:nodejs:
      NodeCommand: "npm start"
      NodeVersion: 8.8.1
